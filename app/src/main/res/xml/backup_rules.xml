<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude DataStore files (sensitive user data) -->
    <exclude domain="file" path="datastore" />
    <!-- Include other app files -->
    <include domain="file" path="." />
</full-backup-content>
