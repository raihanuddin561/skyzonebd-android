<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Production configuration for release builds -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust system certificates -->
            <certificates src="system" />
            <!-- Trust user-added certificates (for debugging) -->
            <certificates src="user" />
        </trust-anchors>
    </base-config>
    
    <!-- Only allow HTTPS traffic -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">skyzonebd.vercel.app</domain>
    </domain-config>
    
    <!-- Debug configuration - only active in debug builds -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
            <certificates src="system" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
